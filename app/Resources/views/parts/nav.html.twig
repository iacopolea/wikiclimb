<nav class="navbar ">
    <div class="navbar-brand">
        {% if is_granted('ROLE_USER') %}
            <a class="navbar-item" href="{{ path('fos_user_security_logout') }}">Logout</a>
        {% else %}
            <a class="navbar-item" href="{{ path('fos_user_security_login') }}">Login</a>
            <a class="navbar-item" href="{{ path('fos_user_registration_register') }}">Register</a>
        {% endif %}
        <div class="navbar-burger burger" data-target="navMenubd-example">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </div>

    <div class="navbar-menu">
        <div class="navbar-start">

        </div>
        <div class="navbar-end">

        </div>
    </div>
</nav>